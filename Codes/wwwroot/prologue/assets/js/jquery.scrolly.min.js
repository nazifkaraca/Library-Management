/* jquery.scrolly v1.0.0-dev | (c) @ajlkn | MIT licensed */
(function (e) {
    function u(s, o) {
        var u, a, f;
        if ((u = e(s)).length == 0) return null;
        a = u.offset().top;
        switch (o.anchor) {
            case "middle":
                f = a - (e(window).height() - u.outerHeight()) / 2;
                break;
            default:
            case "top":
                f = Math.max(a, 0);
        }
        return typeof o.offset == "function" ? f -= o.offset() : f -= o.offset, f;
    }
    var t = "length",
        n = null,
        r = "top",
        i = "offset",
        s = "click.scrolly",
        o = e(window);
    e.fn.scrolly = function (i) {
        var o, a, f, l, c = e(this);
        if (this[t] == 0) return c;
        if (this[t] > 1) {
            for (o = 0; o < this[t]; o++) e(this[o]).scrolly(i);
            return c;
        }
        l = null, f = c.attr("href");
        if (f.charAt(0) != "#" || f[t] < 2) return c;
        a = jQuery.extend({
            anchor: r,
            easing: "swing",
            offset: 0,
            parent: e("body,html"),
            pollOnce: !1,
            speed: 1000 // Varsayılan hız: 300ms
        }, i);
        a.pollOnce && (l = u(f, a));
        c.off(s).on(s, function (e) {
            var t = l !== null ? l : u(f, a);
            t !== null && (e.preventDefault(), a.parent.stop(true).animate({
                scrollTop: t
            }, a.speed, a.easing));
        })
    }
})(jQuery);
